# Fetch Censys ASM host asset(s) by IP. Pipeline: ip_field=. No pipeline: ip= (comma-separated). Throttle constants are in censys_asm_hosts_command.py.

[censysasmhosts]
filename = censys_asm_hosts_command.py
python.version = python3
chunked = true
enableheader = true
requires_srinfo = true
