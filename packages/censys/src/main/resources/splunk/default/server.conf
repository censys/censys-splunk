# Replicate the app's custom configuration across search head cluster members.
# splunk_create.conf is the only custom .conf shipped by this app; Splunk does
# not replicate custom configuration files unless they are listed here.
[shclustering]
conf_replication_include.splunk_create = true
